.header
  -if can? :edit, @user
    =link_to t(:edit), edit_user_path(@user), {class: 'btn action-button btn-default pull-right', role: 'button'}
  %h2=@user.name

=render partial: 'user_display', locals: {user: @user}