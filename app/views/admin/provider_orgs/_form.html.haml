= simple_form_for([:admin, @provider_org], html: { class: 'form-horizontal'}) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name

  .form-actions
    = f.button :submit, 'Save'
    = link_to 'Cancel', :back
