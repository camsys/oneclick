%i.icon-exclamation-sign
= "#{pluralize(@planned_trip.hidden_itineraries.size, 'option')} hidden."
= link_to unhide_all_user_trip_planned_trip_path(current_user, @trip, @planned_trip) do
  Unhide all
