.navbar
  %div.responsive-mbp.row-fluid.navbar-inner
    %a.back-button.pull-left{href: back_url}
      .arrow
        .arrow-btn
        %span Back

    %a.btn.btn-navbar{data: { toggle: "collapse",  target: ".nav-collapse"}}
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
        -#<!-- Everything you want hidden at @navbarCollapseWidth or less, place within here -->
    .nav-collapse.collapse.pull-right
      %ul.nav.pull-right
        - if user_signed_in?
          %li
            = link_to destroy_kiosk_user_session_path do
              = t(:logout)
          /%li
          /  = link_to ("Help"), error_501_path
        - else
          / %li
          /   = link_to new_user_session_path do
          /     = t(:log_in)
          /%li
          /  = link_to ("Help"), error_501_path
