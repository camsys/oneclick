.attract-container
  .attract-header
    .oneclick.pull-left
    .penndot.pull-right
  - @actions.each do |action|
    - Rails.logger.info "home.html.haml, action is #{action.ai}"
    %a.attract-main.row-fluid{alt: action[:label], href: action[:target]}
      .title Touch to begin
    %a.attract-footer{alt: action[:label], href: action[:target]}


:javascript
  $('.navbar').hide();

