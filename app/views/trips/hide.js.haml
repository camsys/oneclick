$('#itineraries').html("#{escape_javascript(render :partial => 'trip_itineraries', :locals => {:itineraries => @trip.valid_itineraries, :selected => @next_itinerary})}");
$('#hidden_trips_notice').html("#{escape_javascript(render 'hidden_trips_notice')}");
fix_thumbnails();
