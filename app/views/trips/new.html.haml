%h1
  %i.icon-bus-sign
  = t(:plan_a_trip)
  %h4
    -if @trip.errors.any?
      %div{:class => "alert alert-error"}
        %a.close{"data-dismiss" => "alert"} ×
        %div= t(:correct_errors_to_create_a_trip)
= render 'form'

