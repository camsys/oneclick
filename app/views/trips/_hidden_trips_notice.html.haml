- if trip_part.itineraries.valid.hidden.count > 0
  %i.icon-exclamation-sign
  = t(:trip_options_hidden, :count => trip_part.itineraries.valid.hidden.count)
  = link_to unhide_all_user_trip_part_path(@traveler, trip_part) do
    = t(:unhide_all)
