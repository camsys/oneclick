%table
  %tr
    %td.itinerary_icons
      %i.fa-play.mode_icon_formatting{:class => 'start_icon'}
    %td
      .itinerary_text
        %p= t(:start) + itinerary.trip_part.from_trip_place.name
      .itinerary_text_small
        = format_time(itinerary.start_time.in_time_zone)

  -itinerary.get_legs.each do |leg|
    %tr
      %td.itinerary_icons
        %i.transit_itinerary_icons.mode_icon_formatting{:class => get_mode_icon(leg.mode)}
      %td
        .itinerary_text
          %p= leg.mode + ' ' + t(:to) + ' ' + leg.end_place.name
        .itinerary_text_small
          =duration_to_words(leg.duration)
  %tr
    %td.itinerary_icons
      %i.fa-stop.mode_icon_formatting{:class => 'end_icon'}
    %td
      .itinerary_text
        %p= t(:arrive) + itinerary.trip_part.to_trip_place.name
      .itinerary_text_small
        = format_time(itinerary.end_time.in_time_zone)
