// Remove existing messages
remove_messages();
// Show any new messages
$('#messages').html("#{escape_javascript(render 'shared/messages')}");
// Refresh the traveler table
$('#traveler_table').html("#{escape_javascript(render 'devise/registrations/traveler_table')}");
// Update the tab label
$("a[data-id='3']").text("#{t(:travelers) + ' (' + current_user.travelers.count.to_s + ')'}");
